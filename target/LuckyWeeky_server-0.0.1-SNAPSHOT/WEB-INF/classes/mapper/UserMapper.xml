<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.ssafy.luckyweeky.domain.user.repository.UserMapper">
    <!-- user_id로 사용자 조회 -->
    <select id="findById" parameterType="long" resultType="io.ssafy.luckyweeky.domain.user.model.User">
        SELECT * FROM User WHERE user_id = #{userId}
    </select>

    <!-- 이메일로 사용자 조회 -->
    <select id="findByEmail" parameterType="string" resultType="io.ssafy.luckyweeky.domain.user.model.User">
        SELECT * FROM User WHERE email = #{email}
    </select>

    <!-- 사용자 추가 -->
    <insert id="insertUser" parameterType="io.ssafy.luckyweeky.domain.user.model.User">
        INSERT INTO User (user_id, username, email, password_hash, created_at, updated_at)
        VALUES (#{userId}, #{username}, #{email}, #{passwordHash}, #{createdAt}, #{updatedAt})
    </insert>
</mapper>
